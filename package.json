{
  "name": "dobutton",
  "version": "1.0.0",
  "description": "A 'Deploy to Heroku' style button for DigitalOcean",
  "scripts": {
    "install": "bower install",
    "deploy": "surge --project ./static --domain dobutton.surge.sh"
  },
  "devDependencies": {
    "bower": "*",
    "surge": "*"
  }
}
