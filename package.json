{
  "name": "do-it",
  "version": "1.0.0",
  "description": "A 'Deploy to Heroku' style button for DigitalOcean",
  "scripts": {
    "install": "bower install",
    "deploy": "surge --project ./public --domain do-it.surge.sh"
  },
  "devDependencies": {
    "bower": "*",
    "surge": "*"
  }
}
